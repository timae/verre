# Aroma layer — Verre integration proposal

**Status: decisions settled (§10 all DECIDED, 2026-07-08); PR A in progress.** Adds the aroma **descriptor** layer (Layer 2 of [`tasting-model-brief.md`](tasting-model-brief.md)) to Verre's impressions. The structure-wheel (Layer 1) shipped this as far as "Aroma = intensity of the smell"; this proposal is the "*what* it smells like" half — the universal descriptor tree in [`aroma-taxonomy.json`](aroma-taxonomy.json).

Scope guard: **wine only, as Verre exists today.** The tree is category-universal by design, so nothing here blocks coffee/cheese later — but no category work ships with this. The brief's Layer 1 material (category baselines, Warmth→Spiciness swap) is context, not scope; drift it creates is flagged in §9.

## 1. What ships

Per rating (session rate AND standalone check-in), a set of aroma selections. Each selection is the pair the brief mandates:

```
{ a: <node id — any tier>, m: <modifier id> | null, p?: true }
```

- `a` — a taxonomy node id at **any tier** (leaf `strawberry`, subfamily `fruity.berry`, family `fruity`). **Any-tier ruling (Simon, 2026-07-08, supersedes this proposal's original leaf-only rule):** a lazy or unsure taster picking "berries" or even "fruity" is honest coarse data — "tastes like cooked berries" is a reality — and forcing a leaf fabricates false precision, which poisons the spread signal. The stored grain encodes the taster's confidence. No auto-subsumption: `fruity.berry` + `strawberry` may coexist ("berries, and specifically strawberry").
- `m` — one of the 8 modifiers, or null for the fresh/default state. Gating per tier: a **leaf** takes its effective declared set (leaf ← subfamily ← family downward inheritance); a **subfamily/family** takes the **union of its descendants' effective sets** — the brief's "modifiers inherit upward" rule applied to selectability (Simon's catch, 2026-07-08). So "overripe fruit" is valid because Berry allows `ripe`, while "pickled Fruity" stays 400 because no Fruity descendant allows `pickled` — nonsense-prevention is automatic, nothing is hand-authored at coarse tiers. Never fused into the node token (`"dried-fig"` is banned by the brief's data model — fusing destroys the modifier aggregation axis).
- `p` — **pronounced flag**: marks the note(s) that led the impression. (Ruled as the "dominant" flag `d` 2026-07-08; display word DECIDED **"Pronounced"** and the wire field re-keyed `d`→`p` to match, Simon 2026-07-10 — done while zero stored data existed, so no migration. The rename window is now closed: a future display-word re-pick is copy-only, the field stays `p`. The gate rejects a payload carrying the old `d` key loudly — no released client ever sent it, so its presence is always a stale script/build, and silent dropping would lose the signal.) Optional boolean on the wire; canonical/stored form carries it only when `true` (absent = not pronounced, `false` normalized away). Not part of the dedupe identity (duplicate `(a, m)` pairs merge, pronounced wins) and never an extra cap unit. Server puts NO ceiling on how many selections are pronounced — if emphasis inflation becomes a problem it's a client-UX concern, not a validation rule.

Faults are neutral leaves like any other (Chemical family). No good/bad flag anywhere.

## 2. Taxonomy home: `packages/core`

The tree is static data + pure derivations — exactly what `@verre/core` exists for (platform-pure, shared web↔native). New module `packages/core/src/aroma/`:

- `taxonomy.ts` — the tree as typed data (generated from the JSON, or the JSON imported + typed; either way the JSON in this folder stops being canonical the day this lands).
- Derivations (all pure, all computed from tree position, **never by parsing ids** — see §3; as-built names): `getAromaLeaf(id)` + `aromaTierPath(id)` (leaf-grain), **`getAromaNode(id)` — the any-tier resolver (#6), the read primitive for coarse selections**, `aromaAllowedModifiers(id)` (any tier: leaves by declared inheritance, coarse nodes by descendant-union), `aromaModifierDisplay(nodeId, modId)` (per-leaf display-word override, e.g. strawberry+cooked → "jammy"; non-leaf falls through to the default label), `isValidAromaSelection(a, m)`, and `gateAromaSelections` (§5 — incl. the #8 promoted-percept canonicalization).
- `searchAromas()` (`aroma/search.ts`) — **built on `feature/aroma-input` (PR B, 2026-07-10)**; the proposal called it `searchIndex()`: flat index over node labels (all tiers, since #6) + **per-leaf `search_aliases`** (seeded 2026-07-09: 83 aliases on 64 leaves — spelling variants, UK/US terms, trade vocabulary: cassis→blackcurrant, sultana→raisin, pyrazine→green bell pepper, lime blossom→linden blossom; CI enforces no-label-shadowing + uniqueness) + modifier labels + modifier `search_aliases` + `modifier_display` words, so "dried fig" resolves to `fig + dried` and "jammy strawberry" to `strawberry + cooked`. ⚠️ Four subfamily labels equal leaf labels (`honey`, `vanilla`, `cocoa`, `char` — e.g. `sweet.honey` vs the honey leaf): ids stay unambiguous, but search/UX must disambiguate the two selectable nodes, and mixed-grain counting (compare milestone) should treat an eponymous coarse-vs-leaf pick as near-duplicate.

Size: the JSON is 65KB pretty-printed, ~25–30KB minified inside a bundle. Acceptable for both web and native; native *needs* it locally for the browse tree anyway.

Validation status of the v1 file (checked 2026-07-08): ids unique, labels unique tree-wide (the "one leaf, one home" rule holds), every `allowed_modifiers` / `modifier_display` reference resolves, every leaf has an effective allowed-modifier set via inheritance, promoted leaves (raisin, prune) present, brief's contested placements all match (blue cheese → Funky, flint → Mineral vs struck-match → Chemical, dairy split, cured veg, peat → Fire). A CI check should re-run these invariants once the file lives in core (same shape as the palette gate precedent).

**Brief-vs-built audit content fixes (2026-07-09, on `feature/aroma-any-tier`)** — a completeness audit found four v1 authoring gaps against the brief's own MODIFIERS rules; fixed in the canonical JSON: raisin/prune overridden to `['cooked']` (promotion rule — no more redundant `raisin + dried`; stewed prunes stay expressible); `vegetal.cured` → `[]` (olive/caper/pickle ARE the preserved percept); `spice.pungent` gains `smoked` (the brief's own "smoked chili → Spice" example was inexpressible); `vegetal.green/stalky` gain `cooked` (the brief's "cooked plant notes route via modifier on the fresh base" promise — stewed rhubarb, with a `stewed` display word; `herbs` was initially included and reverted per Simon 2026-07-09 — no point to cooked herbs).

## 3. ID scheme — DECIDED shape (pending Simon's confirm): path-free leaf slugs

The JSON's `id_scheme` bakes the tree path into the id (`fruity.citrus.lemon`). The brief simultaneously promises: *"If the taxonomy is manually re-parented later, historical impressions inherit the new structure automatically."* **These two contradict.** Re-parenting a leaf either changes its id (breaking every stored `(aroma_id)` reference — the exact failure the promise rules out) or leaves the id lying about its path (breaking anything that derives tiers by splitting the id, which the dotted scheme invites).

Implementation reality check (why the dotted scheme buys nothing): in code the id is an **opaque Map key** — `getLeaf`, `allowedModifiers`, `tierPath`, the search index, chip render, the `gateAromas` 400-check are all lookups into structures built from the tree at module load. Nothing legitimate ever parses an id; the only code path-encoding enables is the id-splitting shortcut we'd have to forbid anyway. So the id's *only* real property is stability under taxonomy evolution — and path-encoding is the one choice that breaks it.

**The scheme:**

- **Leaf ids = label-derived slugs** (`lemon`, `blue_cheese`, `pumpkin_seed`), stored in impressions. Uniqueness is guaranteed by the one-leaf-one-home rule (labels are unique tree-wide; where a percept splits, the brief already mandates *different words*). One fix needed in the v1 JSON: `kernel.seeds.pumpkin` shortened its slug and collides with the vegetable — becomes `pumpkin_seed` (its label already says "pumpkin seed").
- **Family + subfamily ids stay qualified** (`fruity`, `mineral.stone`). Verified: bare subfamily slugs actually collide (`stone`, `cured`, `dairy` each exist twice), and 6 leaf slugs coincide with subfamily slugs (`char`, `honey`, `vanilla`, `cocoa`, `anise`, `petrol`) — the dot dichotomy keeps them unambiguous. ⚠️ **Since the any-tier ruling (#6), tier-1/tier-2 ids ARE persisted when a taster selects at coarse grain** — which re-imports a slice of the path-encoding fragility this section eliminated for leaves: re-parenting a subfamily (or renaming a family, which re-ids every subfamily under it) changes stored tier-2 ids. Accepted trade-off: tier-2 moves are far rarer than leaf moves, and the `superseded_by` alias mechanism (next bullet) covers them the same way.
- **Namespace rule** (revised by the any-tier ruling): a stored aroma reference may name a node at any tier, and stays unambiguous by construction — leaf slugs never contain a dot, subfamily ids always do, and leaf slugs are CI-enforced disjoint from family slugs. *Computed* roll-up grain for compare remains a query-time parameter, never a stored value; what IS stored is the grain the taster actually chose.
- **Rename ≠ re-id**: a label rename keeps the slug (slug drift from label is acceptable; the label is the display truth). Deleting/merging a node later — and, since #6, re-parenting a subfamily or renaming a family (both re-id stored tier-2 references) — needs an explicit `superseded_by` alias map in the taxonomy. True under ANY id scheme, so it's a content-pass mechanism, not a reason to prefer either scheme.
- Modifier ids (`dried`, `cooked`, …) are already opaque and stay as-is.

The dotted form survives as a *derived* path string (`tierPath(id).join('.')`) for debugging/display, never as a key. The JSON gets regenerated with slug leaf-ids as part of PR A (a mechanical transform + the one pumpkin fix); the `conventions.id_scheme` note updates accordingly.

Rejected alternative: keep dotted ids and declare re-parenting a breaking change requiring stored-data migrations. Preserves the current JSON verbatim but makes the brief's re-parenting promise false — and the promise is load-bearing, because the brief explicitly plans content passes after data exists.

## 4. Data model

Rides the existing two-tier persistence — aromas are part of the rating, not a new entity:

- **Redis** (`s:{CODE}:r:{ID}:{WINEID}` JSON): add `aromas: [{a, m, p?}]` beside `score`/`flavors`/`notes`. Absent/empty array = none.
- **Postgres**: `ratings.aromas Json @default("[]")` — additive column, non-destructive migration, mirrors the `flavors` precedent. Same column serves standalone check-ins.
- **Present-replaces / omitted-preserves** (as built in PR A): the rate POST treats an OMITTED `aromas` field as "keep what's stored" (Redis via a prior-value read; Postgres via a `CASE` on the conflict row, so a TTL'd Redis key can't wipe the archive) and a PRESENT field — including `[]` — as the full new truth. Rationale: the rate POST is otherwise full-replace, but web's rate pane and any stale binary won't send `aromas` for a while, and a save from them must not wipe selections made on native. Consequence for "clear my rating": an empty POST from an aromas-unaware client leaves an aromas-only row alive (correct — don't destroy data the client doesn't know about); the explicit Reset DELETE force-reaps everything.

**Why a JSON column, not a `rating_aromas` table**: consistency with `flavors` and the Redis→PG archival flow; the read paths that matter (rating render, feed card, compare) always want the whole set for a rating; and the brief's roll-ups are computed *from the taxonomy at query time*, so relational tier columns would be denormalised derivations we'd have to keep in sync — the thing the brief's "derive on demand" rule exists to prevent. Revisit trigger: the Palate Fingerprint profile aggregate — if per-user cross-rating slicing in SQL gets hot, a projection table can be added *then* without touching the write model (`lib/profileFlavor.ts` already does this style of aggregation over `flavors` JSON in app code).

**Cap**: 30 selections per rating (DECIDED, Simon 2026-07-08 — a validation limit, generous vs real tasting-note practice; prevents a hostile 10k-element array). **Unit = one selection = one `(a, m)` pair** — the modifier is a field on the selection, never a second unit, and per-selection flags are likewise fields, not units (the pronounced flag `p` shipped exactly this way — no migration, as predicted here). Dedupe on the `(a, m)` pair — `fig` and `fig + dried` are two distinct selections (legitimately: fresh and dried fig notes in one wine).

**Engagement rule extension (mandatory touchpoint)**: `hasEngagement` in the rate POST and the empty-payload predicate in `lib/engagementCascade.ts` both currently define engagement as score > 0 OR non-empty flavours OR non-empty note. A rating carrying only aromas MUST count as engaged, and an upsert that empties aromas along with everything else must still trigger the cascade. Both sides of the predicate change in the same PR that adds the column — shipping the column without this silently breaks the feed-item lifecycle.

**Cascade/tombstone**: nothing new. Aromas live inside the `ratings` row, so they follow the existing split rule (standalone hard-cascade, session tombstone) with zero extra handling. No S3, no counters, no new rate limits (writes ride the existing rate/checkin endpoints and their caps).

## 5. Validation chokepoint

Mirrors the flavours pattern exactly — one pure validator in core, one server gate in lib, applied by every write route AFTER it:

- **core `validateAromaSelections(input)`** — shape check: array, ≤ cap, each element `{a: string, m: string|null, p?: boolean}` (`p` normalized to present-only-when-true), dedupe. Shared with native for optimistic state.
- **lib/aromas.ts `gateAromas(selections)`** — the server boundary: `a` must be a known taxonomy node id (any tier) → else **400, loudly** (`unknown aroma id`); `m` must be null or in the node's effective `allowedModifiers(a)` → else **400** (`modifier not allowed for this aroma`). No silent stripping: unlike the flavours gate (where a stray zero is a fill artifact from a style race), there is no innocent way to post an unknown node id or a disallowed modifier — reject is always correct. Returns the deduped canonical array; the cap is checked on the RAW input length (before dedupe) — stricter than strictly necessary, fine for an abuse bound.

Call sites, phased per §11: `app/api/session/[code]/rate/route.ts` (**wired in PR A**); `app/api/checkins/route.ts` POST + `app/api/checkins/[id]/route.ts` PATCH (**wired in PR C** — POST treats omitted as `[]`, PATCH honours present-replaces / omitted-preserves). Wire format is plain strings/null — no Decimal trap.

## 6. Input UI (native first)

Per the brief's input model: a **modifier bar** + tier-by-tier browse + search. Phasing:

1. **Native input** — **BUILT (PR #77, `feature/aroma-input`, 2026-07-10)**: the 02e·11 search-first section on the impression screen; interaction rulings recorded in `docs/design/decisions/0008-aroma-input-search-first.md`, components catalogued in `apps/mobile/CLAUDE.md`. The browse sheet ships FIVE picker variants for device testing (the four handoff variants Map/Rings/Rail/Canvas + a List variant added on a device ask), committed on `feature/aroma-pickers`.
2. **Web read-only** — **BUILT (PR D, 2026-07-12, §11.5)**: feed + profile render via `components/ui/AromaReadChips.tsx` from the same core helpers (the wine modal's read-only pane turned out to have no consumers — nothing to mount there). Web *input* deferred (web rate UI is slated for redesign anyway — don't build the browse tree twice).
3. **Compare/aggregate** — later phase (§8).

Design detail (chip layout, where the modifier bar sits, wheel-vs-list browse) is deliberately not specified here — that's a `docs/design/` conversation with mockups, and per the pixel-spec rule it needs Simon's visual ruling, not an approximation.

## 7. Display + palette mapping (decision needed)

Family colour comes from the palette's per-theme `aroma` block (12 colours, authored 2026-07-01, `apps/mobile/src/theme/flavour-palette/palette.js`). The palette's family names had drifted from the taxonomy — palette said `Nut/Cocoa` and `Roasted`; the brief/taxonomy renamed these to **Kernel** and **Fire**. **DECIDED (Simon, 2026-07-08): re-key the palette** — `Nut/Cocoa`→`Kernel`, `Roasted`→`Fire`, same colour values, in both the tracked `palette.js` and the gitignored design source `.local/design/flavour-palette.js` (keep the mirrors in sync). Lands with PR A.

Read surfaces render selections grouped by family, family-coloured chips, `modifierDisplay` word as the badge. Blind sessions: no new redaction — aroma selections are the *taster's own perception*, same trust class as structure ratings and notes, and never identify the wine's label.

## 8. Comparison / roll-up (later phase, design constraint now)

The brief's query-time roll-up (strip modifiers → climb tiers → find the agreement grain) is the compare feature's job, not v1's. v1 only has to not foreclose it — which the storage rules already guarantee: node-grain (the taster's chosen tier, per #6) + separate modifier stored, tier paths derived. Because every read resolves the stored node fresh from the tree (`getAromaNode`; one-leaf-one-home makes it deterministic), "1 picked strawberry, 1 peach, 1 cherry → 3 say Fruity" is a pure computation over stored node ids — a coarse `fruity.berry` selection is simply data that arrives pre-collapsed one tier (its counting weight vs a leaf pick is the deferred mixed-grain question, #6) — and a later re-parenting automatically re-aggregates historical data under the leaf's new home. `compareAggregate.ts` grows an aroma section when the compare milestone picks this up.

## 9. Adjacent drift flagged (NOT this proposal's scope)

- **Structure master list — RULED (Simon, 2026-07-08): Spiciness is in, Warmth is dropped.** Palette swap LANDED 2026-07-09 (`Warmth`→`Spiciness` re-key in both palette copies + d.ts + the compare person-series label order, keeping the hexes — chili-red suits Spiciness; Simon may re-pick values later). PERSON_SERIES colours are byte-identical.
- **Wine gains FUNK + a new display order — RULED + LANDED (Simon, 2026-07-09).** The brief's wine baseline includes Funk; the shipped wine-7 predated it (a §9 drift this doc initially missed). New registry order: **Sweetness · Acidity · Smell · Taste · (Bubbles, in-position on spark) · Funk · Tannin · Body · Finish** — a deliberate re-order of the shipped wedge layout, superseding the original append-only lock. Storage untouched (keyed map); pre-Funk rows lack the key → no spoke (Bubbles precedent); zero-fill/gate pick Funk up from the registry automatically.
- **Category baselines / Drink-Food hierarchy** (brief Layer 1): future-categories work. `resolveAxes` already reserves the `category` parameter for it.
- **Taxonomy content pass** (brief's open work #1): the v1 tree is shippable as-is (per-node modifier gating is already authored throughout, contra the brief's own open-work note, which predates this JSON). Deeper leaves (hop varietals, cheese rinds) are additive content PRs against the core file — never blockers. (Per-leaf search aliases: field + first 83 entries seeded 2026-07-09, see §2.)

## 10. Decision registry

1. ~~**ID scheme** (§3)~~ — **DECIDED (Simon, 2026-07-08): path-free label-derived leaf slugs**, qualified tier-1/tier-2 ids, `pumpkin_seed` collision fix. (The "leaf-only persistence" part of this decision was superseded the same day by **#6** — any-tier selection.) Performance was probed and answered (O(1) map lookups built at module load; the lost `LIKE 'fruity.%'` SQL shortcut is the flaw, not a feature — app-side aggregation or a taxonomy-derived VALUES join covers any future SQL need re-parent-safely).
2. ~~**Palette family re-key** (§7)~~ — **DECIDED (Simon, 2026-07-08): re-key to `Kernel`/`Fire`** in both palette copies. Bonus ruling same day: structure list takes `Spiciness`, drops `Warmth` (§9).
3. ~~**Selection cap** (§4)~~ — **DECIDED (Simon, 2026-07-08): 30** (abuse bound, not a UX target; §4 defines the unit — one `(a, m)` pair = 1).
4. ~~**v1 surface cut** (§6)~~ — **DECIDED (Simon, 2026-07-08): moments write route first** (`/api/session/[code]/rate`), then the standalone check-in routes as a follow-up carrying the lessons learned. Read surfaces follow the same order.
5. ~~**Blue cheese in Funky**~~ — **CONFIRMED (Simon, 2026-07-08): stays in Funky.** The brief's open #3 is closed; the single Funky cheese exception is canonical.
6. ~~**Any-tier selection**~~ — **DECIDED (Simon, 2026-07-08, post-PR-A): selections may reference a node at ANY tier**, not leaves only ("tastes like cooked berries" is a reality; forced leaf precision fabricates data). Modifier gating: leaves by declared inheritance, coarse nodes by descendant-union (upward inheritance — see §1); no auto-subsumption; mixed-grain counting semantics deferred to the compare milestone. Shipped as the `feature/aroma-any-tier` follow-up (gate widening — every previously-valid payload stays valid; zero stored data existed).
7. ~~**Dominant flag**~~ — **DECIDED (Simon, 2026-07-08): a selection can be marked dominant** (`d: true`, §1). Same follow-up branch as #6. Uncapped server-side. **Display word DECIDED (Simon, 2026-07-10): "Pronounced"** — and the wire field re-keyed `d`→`p` the same day while zero stored data existed (§1; the rename window is closed).
8. ~~**Promoted-percept dual encoding**~~ — **DECIDED (Simon, 2026-07-09): canonicalize at the gate.** `grape + dried` → `raisin`, `plum + dried` → `prune`, via `promoted_from: {a, m}` authored on the promoted leaf. The gate rewrites the composite AFTER validity (must be legal on the base) and BEFORE dedupe (composite + promoted forms in one payload merge, pronounced wins), so one percept has exactly one stored encoding. CI validates every mapping: base is a real leaf, composite legal on the base (no dead canonicalizations), and the promoted leaf must NOT allow its own promoted-from modifier (keeps the redundant pair impossible). Future promotions = author the leaf + its `promoted_from`; no code change. (Same ruling round: `cooked` reverted from `vegetal.herbs` — no point to cooked herbs; green/stalky keep it.)

## 11. Rollout shape

1. Decision #1 (id scheme) confirmed in this doc — the last open gate before PR A.
2. PR A (backend, moments-first) — **BUILT (2026-07-08, `feature/aroma-core`)**: core taxonomy module (slug-id regeneration + pumpkin_seed fix) + validators, palette re-key (both copies), `ratings.aromas` column + migration, `gateAromas`, **session rate route only**, engagement predicate extension, session GET surfaces include `aromas`. CI taxonomy-invariants check (`scripts/check-aroma-taxonomy.mjs`). E2E pins in `.local/test-env/scripts/section-aromas.sh` (22 assertions: gate 400s, dedupe, omitted-preserves on both stores, aromas-as-engagement, extended cascade predicate).
3. PR B (native input) — first slice **MERGED (PR #77, 2026-07-10)**: `d`→`p` re-key, core `searchAromas`, the search-first input + Rail picker (ADR-0008). Second slice **MERGED (PR #78, 2026-07-12)**: the remaining Rings/Map/Canvas pickers + a List variant — all FIVE ship behind the browse sheet's segmented control for on-device feel-testing; one gets ruled after device time (element decisions deferred pending external feedback). Also folded in: the Structure input/wheel rename + badge-tint ruling.
4. PR C (standalone check-ins) — **BUILT (2026-07-12, `feature/aroma-checkins`)**: `/api/checkins` POST + `[id]` PATCH gain `aromas` (same `gateAromas` chokepoint; POST omitted = `[]`, PATCH present-replaces / omitted-preserves), feed + profile read paths ship `aromas` on both payload kinds (unredacted per §7), `AromaInput` on the check-in create rate stage, read-only `AromaReadChips` on the feed impression detail, README API rows. Also carried on the branch: the shared `RatingSection` (one rating block for impression + check-in), the armed-search ruling (ADR-0008 addendum — armed keeps resting colours, siblings mute), and the iOS 26 edge-only back-gesture opt-out. E2E pins in `.local/test-env/scripts/section-aromas-checkins.sh` (24 assertions).
5. PR D (remaining read surfaces) — **BUILT (2026-07-12, `feature/aroma-web-chips`)**: web read-only chips (`components/ui/AromaReadChips.tsx`, the native primitive's web twin — same §7 display order/labels, provisional family palette in `lib/flavours.ts` beside `WEB_PALETTE`) mounted on the feed + profile check-in cards (`CheckinCard`) and every session-post wine row (`SessionFeedCard`, blind rows included — no redaction fork). Wire parity had already shipped in PR C (profile + feed payloads carry `aromas`); render-only, no schema, no new endpoints. Web *input* stays deferred to the redesign (§6). Out of scope, confirmed empty: the wine modal's read-only rating pane has no consumers, and the web compare page waits for the §8 roll-up.
6. Later milestones: compare roll-up, Palate Fingerprint, content passes.

Each PR on its own `feature/aroma-*` branch, reviewer pass per root rules (schema + shared primitive → mandatory).
